# AutoPrep AI - backend dependencies (Python 3.12+)

fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
pandas==2.2.3
python-multipart==0.0.20
# Optional but recommended for mutual information; numpy fallback is used if unavailable
scikit-learn>=1.5.0,<1.7

# Frontend (kept here so a single install covers the whole project)
streamlit==1.41.1
requests==2.32.3

# Tests
pytest==8.3.4
